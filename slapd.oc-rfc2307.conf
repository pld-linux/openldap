
objectclass posixAccount
          requires cn, uid, uidNumber, gidNumber, homeDirectory
          allows userPassword, loginShell, gecos, description

objectclass shadowAccount
          requires uid
          allows userPassword, shadowLastChange, shadowMin,
                shadowMax, shadowWarning, shadowInactive,
                shadowExpire, shadowFlag, description

objectclass posixGroup
          requires cn, gidNumber
          allows userPassword, memberUid, description

objectclass ipService
          requires cn, ipServicePort, ipServiceProtocol
          allows description

objectclass ipProtocol
          requires cn, ipProtocolNumber
          allows description

objectclass oncRpc
          requires cn, oncRpcNumber, description
          allows description

objectclass ipHost
          requires cn, ipHostNumber
          allows l, description, manager

objectclass ipNetwork
          requires cn, ipNetworkNumber
          allows ipNetmaskNumber, l, description, manager

objectclass nisNetgroup
          requires cn
          allows nisNetgroupTriple, memberNisNetgroup, description

objectclass nisMap
          requires nisMapName
          allows description

objectclass nisObject
          requires cn, nisMapEntry, nisMapName
          allows description

objectclass ieee802Device
          allows macAddress

objectclass bootableDevice
          allows bootFile, bootParameter
